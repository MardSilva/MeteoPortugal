<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Name="SettingsPageRoot"
    x:Class="MeteoPortugal.App.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="Definições"
    BackgroundColor="{AppThemeBinding Light={StaticResource ColorBackground}, Dark={StaticResource DarkColorBackground}}">
    <Grid Padding="24" RowDefinitions="Auto,*,Auto" RowSpacing="18">
        <VerticalStackLayout Spacing="6">
            <Label
                FontFamily="{StaticResource FontFamilyBold}"
                FontSize="30"
                Text="{Binding Title}"
                TextColor="{AppThemeBinding Light={StaticResource ColorTextPrimary}, Dark={StaticResource DarkColorTextPrimary}}" />
            <Label
                FontSize="15"
                Text="Ajuste a cidade principal e a aparência da aplicação."
                TextColor="{AppThemeBinding Light={StaticResource ColorTextSecondary}, Dark={StaticResource DarkColorTextSecondary}}" />
        </VerticalStackLayout>

        <Border Grid.Row="1" Padding="18" Style="{StaticResource WeatherCardBorder}">
            <Grid RowDefinitions="Auto,Auto,*" RowSpacing="12">
                <Grid ColumnDefinitions="*,Auto">
                    <VerticalStackLayout Spacing="2">
                        <Label
                            Style="{StaticResource SectionTitleLabel}"
                            Text="Cidade padrão" />
                        <Label
                            FontSize="13"
                            Text="A Home usa esta localidade sempre que abre."
                            TextColor="{AppThemeBinding Light={StaticResource ColorTextSecondary}, Dark={StaticResource DarkColorTextSecondary}}" />
                    </VerticalStackLayout>
                    <Label
                        Grid.Column="1"
                        FontFamily="{StaticResource FontFamilySemiBold}"
                        FontSize="13"
                        Text="{Binding LocationCount, StringFormat='{0} locais'}"
                        TextColor="{AppThemeBinding Light={StaticResource ColorPrimary}, Dark={StaticResource DarkColorPrimary}}"
                        VerticalTextAlignment="Center" />
                </Grid>

                <SearchBar
                    Grid.Row="1"
                    Placeholder="Pesquisar cidade"
                    Text="{Binding LocationSearchText}"
                    TextColor="{AppThemeBinding Light={StaticResource ColorTextPrimary}, Dark={StaticResource DarkColorTextPrimary}}" />

                <CollectionView
                    Grid.Row="2"
                    ItemsSource="{Binding FilteredLocations}"
                    SelectionMode="None">
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Border
                                Margin="0,0,0,10"
                                Padding="14"
                                BackgroundColor="{AppThemeBinding Light={StaticResource ColorSurfaceAlt}, Dark={StaticResource DarkColorSurfaceAlt}}"
                                StrokeThickness="0">
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="8" />
                                </Border.StrokeShape>
                                <Grid ColumnDefinitions="*,Auto" RowDefinitions="Auto,Auto">
                                    <Grid.GestureRecognizers>
                                        <TapGestureRecognizer
                                            Command="{Binding Source={x:Reference SettingsPageRoot}, Path=BindingContext.SelectLocationCommand}"
                                            CommandParameter="{Binding .}" />
                                    </Grid.GestureRecognizers>
                                    <Label
                                        FontFamily="{StaticResource FontFamilySemiBold}"
                                        Text="{Binding Name}"
                                        TextColor="{AppThemeBinding Light={StaticResource ColorTextPrimary}, Dark={StaticResource DarkColorTextPrimary}}" />
                                    <Label
                                        Grid.Row="1"
                                        FontSize="12"
                                        Text="{Binding District}"
                                        TextColor="{AppThemeBinding Light={StaticResource ColorTextSecondary}, Dark={StaticResource DarkColorTextSecondary}}" />
                                    <Label
                                        Grid.Column="1"
                                        Grid.RowSpan="2"
                                        FontFamily="{StaticResource FontFamilySemiBold}"
                                        Text="Usar"
                                        TextColor="{AppThemeBinding Light={StaticResource ColorPrimary}, Dark={StaticResource DarkColorPrimary}}"
                                        VerticalTextAlignment="Center" />
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </Grid>
        </Border>

        <Border Grid.Row="2" Padding="18" Style="{StaticResource WeatherCardBorder}">
            <VerticalStackLayout Spacing="12">
                <VerticalStackLayout Spacing="2">
                    <Label
                        Style="{StaticResource SectionTitleLabel}"
                        Text="Tema" />
                    <Label
                        FontSize="13"
                        Text="Pode seguir o sistema ou forçar claro/escuro."
                        TextColor="{AppThemeBinding Light={StaticResource ColorTextSecondary}, Dark={StaticResource DarkColorTextSecondary}}" />
                </VerticalStackLayout>

                <Picker
                    ItemsSource="{Binding Themes}"
                    SelectedItem="{Binding SelectedTheme}"
                    Title="Escolha o tema" />
            </VerticalStackLayout>
        </Border>
    </Grid>
</ContentPage>

